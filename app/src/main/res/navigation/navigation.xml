<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/mainFragment">

    <!-- Main fragment screen -->
    <fragment
        android:id="@+id/mainFragment"
        android:name="com.africochat.www.ui.fragments.MainFragment"
        android:label="@string/app_name_label"
        tools:layout="@layout/main_fragment" >
        <action
            android:id="@+id/action_mainFragment_to_signInFragment"
            app:destination="@id/signInFragment"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@anim/fade_out" />
        <action
            android:id="@+id/action_mainFragment_to_settingsFragment"
            app:destination="@id/settingsFragment"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@anim/fade_out" />
    </fragment>

    <!-- Sign in fragment screen -->
    <fragment
        android:id="@+id/signInFragment"
        android:name="com.africochat.www.ui.fragments.SignInFragment"
        android:label="@string/sign_in_fragment_label"
        tools:layout="@layout/sign_in_fragment" >

        <action
            android:id="@+id/action_signInFragment_to_mainFragment"
            app:popUpTo="@id/mainFragment"
            app:popUpToInclusive="false"
            app:destination="@id/mainFragment" />
    </fragment>

    <!-- Settings fragment screen -->
    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.africochat.www.ui.fragments.SettingsFragment"
        android:label="@string/settings_fragment_label"
        tools:layout="@layout/settings_fragment" />
</navigation>